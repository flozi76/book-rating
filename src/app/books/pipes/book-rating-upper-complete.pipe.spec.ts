import { BookRatingUpperCompletePipe } from './book-rating-upper-complete.pipe';

describe('BookRatingUpperCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new BookRatingUpperCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
