import { BookRatingLowerCompletePipe } from './book-rating-lower-complete.pipe';

describe('BookRatingLowerCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new BookRatingLowerCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
